<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= category.name %></title>
</head>
<body>
 
<body class="dreams-container">
    <%- include('../partials/_navbar.ejs') %>

    
    <h1><%= category.name %></h1>

   

<div class="dream-grid"> 
  <% category.dream.forEach(dream=> { %>
    <div class="dream-box"> 
    <a href="/users/<%= currentUser._id %>/categories/<%= category._id %>/dreams/<%= dream._id %>">
    <%= dream.dream %> 
      </a>
    </div>
  <% }) %>
</div>

<div class="actions">
 <!-- link to add dreams inside category -->
    <a id="btn-submit" href="/users/<%= currentUser._id %>/categories/<%= category._id %>/dreams">
        Add Dreams in <%= category.name %>
    </a>

<!-- show the delete and the edit buttons for the category -->
            <form action="/users/<%= currentUser._id %>/categories/<%= category._id %>?_method=DELETE" method="POST">
              <button id="btn-submit" type="submit">Delete</button>
            </form>

            <a id="btn-submit" href="/users/<%= currentUser._id %>/categories/<%= category._id %>/edit">Edit</a>

    <a id="btn-submit" href="/users/<%= currentUser._id %>/categories">Back to categories</a>
</div>
</body>
</html>
