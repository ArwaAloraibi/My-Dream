<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= user?.username || 'User' %>'s Dream</title>
</head>
<body>
 
<h1><%= user?.username || 'User' %>'s Dream</h1>

<ul>
  <% user?.dream.forEach(dream => { %>
    <li> <%= dream.dream %></li>
  <% }) %>
</ul>

<a href="/users">Back to user's index page</a>
</body>
</html>